version: "3.9"

services:
  backend:
    command: npm run dev
    environment:
      NODE_ENV: development

  frontend:
    build:
      target: builder
    command: npm run dev
    ports:
      - "5173:5173"
